providers:
  - name: example-aa
    base_url: https://api.example-aa.com/v1
    auth_token_env: EXAMPLE_AA_TOKEN
    capabilities:
      - planning
      - execution
agents:
  - id: planner_bot
    provider: example-aa
    skills:
      - planning
      - coordination
    metadata:
      tier: beta
  - id: executor_bot
    provider: example-aa
    skills:
      - execution
      - validation
    metadata:
      tier: beta
tasks:
  - id: discovery_brief
    description: Collect customer requirements and produce a placeholder contract.
    required_skills:
      - planning
      - coordination
    sla_minutes: 10
  - id: sanitize_audit
    description: Run the sanitize checklist before any public release.
    required_skills:
      - execution
      - validation
    sla_minutes: 5
ssot_bucket: local
